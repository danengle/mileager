<%= form_for(@fuel_up) do |f| %>
  <% if @fuel_up.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fuel_up.errors.count, "error") %> prohibited this fuel_up from being saved:</h2>

      <ul>
      <% @fuel_up.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :trip %><br />
    <%= f.text_field :trip %>
  </div>
  <div class="field">
    <%= f.label :volume %><br />
    <%= f.text_field :volume %>
  </div>
  <div class="field">
    <%= f.label :cost_per_unit %><br />
    <%= f.text_field :cost_per_unit %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
